# Tomasz Kwiatkowski

Run each file normally with Python 3:

```bash
python3 mt19937.py
python3 pkcs7-pad.py
python3 single-xor.py
```

In single-xor you can change the url (line 41).

In pkcs7-pad you can change the text (line 10).
